<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- إصدار توزيع مباشر فقط: يسمح بفتح مثبت Android بعد تأكيد المستخدم. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
</manifest>
