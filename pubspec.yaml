name: souf_tour
description: Application Android de découverte culturelle et touristique d’El Oued.
publish_to: "none"
version: 1.1.0+2

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^3.4.1
  intl: ^0.19.0
  supabase_flutter: ^2.8.4
  url_launcher: ^6.3.1

dependency_overrides:
  app_links: 6.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
